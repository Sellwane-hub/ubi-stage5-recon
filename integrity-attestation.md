# Stage 5 Integrity Attestation

I built this project step by step and tested it myself as I went, and the git
history shows that. When I got stuck, I used an AI assistant (Claude) to help me
debug and to explain the parts I did not understand yet. By the end I understood
how the pieces fit together and can walk through them. I produced this work under
the Stage 5 rules of engagement.

- The recon engine was built and tested by me; the git history reflects the
  incremental development, tests, failures, and fixes.
- All discovery was performed against the supplied local loopback lab only.
  Only endpoints marked IN in the generated scope were contacted. The
  out-of-scope decoy received zero packets, as shown in `request-ledger.csv`
  and corroborated by the lab's own ledger.
- The engine performs discovery only. The foothold was obtained as a separate,
  deliberate step using values discovered by the engine, and is fully
  transcribed in `foothold-evidence.txt`.
- No private answers were hard-coded. All discovered values (vhost, route key,
  credentials, flag) were obtained live from the running lab.
- The `local_lab.py` runtime was not modified.
- All evidence in this package was generated by the tooling described and is
  reproducible from a clean environment via `make test` and `make run`.

Evidence marker: UBI-A5-6CAABB4AB707
Intern code: UBI-2026-0528
Signed: Sellwane
Date (UTC): 2026-07-21T17:06:38+00:00
